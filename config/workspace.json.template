{
  "_comment": "Minimum config: provide workspaceName OR workspaceId. Other fields are auto-resolved from the current 'az login' context. Add subscriptionId/resourceGroup if the same workspaceName exists in multiple subscriptions you have access to.",
  "workspaceName": "<your-log-analytics-workspace-name>"
}
